import numpy as np
import matplotlib.pyplot as plt

fig, ax = plt.subplots(subplot_kw={'projection': '3d'})

theta = np.linspace(0, np.pi, 0.1)
phi = np.linspace(0, 2*np.pi, 0.1)
R = 5
              
x = phi * cos()
y = 
z = 

ax.plot(x, y, z, label='Dich')

ax.legend()

ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')

ax.set_title('3D test')

plt.savefig('task_1.py')

plt.close()

fig, ax = plt.subplots(subplot_kw={'projection': '3d'})

t = np.arange(0.01, 4*np.pi, 0.1)
R = 1
              
x = R * np.cos(t)
y = R * np.sin(t)
z = R * t

ax.plot(x, y, z, label='Dich')

ax.legend()

ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')

ax.set_title('3D test')

plt.savefig('task_2.py')

plt.close()

fig, ax = plt.subplots(subplot_kw={'projection': '3d'})

t = np.arange(0.01, 4*np.pi, 0.1)
R = 1
              
x = R * np.cos(t)
y = R * np.sin(t)
z = R * t

ax.plot(x, y, z, label='Dich')

ax.legend()

ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')

ax.set_title('3D test')

plt.savefig('task_2.py')

plt.close()

fig, ax = plt.subplots(subplot_kw={'projection': '3d'})

t = np.arange(0.01, 4*np.pi, 0.1)
R = 1
              
x = R * np.cos(t)
y = R * np.sin(t)
z = R * t

ax.plot(x, y, z, label='Dich')

ax.legend()

ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')

ax.set_title('3D test')

plt.savefig('task_2.py')